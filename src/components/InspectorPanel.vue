<script setup>
defineProps({ current: Object, params: Object, state: Object, schema: Object, frameKeys: Array, paramKeys: Array, groupShort: Object, format: String, formats: Array, dark: Boolean, resetParams: Function, randomParams: Function, resetFrame: Function, toggleTheme: Function, setParam: Function, fmt: Function });
defineEmits(['update:format']);
</script>
<template><aside class="insp"><div class="px-4 pt-4 pb-3 border-b"><div class="mono text-[10px]">Параметры</div><h2>{{current.name}}</h2><div class="mono text-[10px]">{{groupShort[current.group]}} · {{paramKeys.length}} контролов</div><div class="grid grid-cols-2 gap-2 mt-3"><button class="btn" @click="resetParams">Сброс</button><button class="btn" @click="randomParams">🎲 Случайно</button></div></div><details class="sec" open><summary>Кадр EQ</summary><div class="sec-body"><div v-for="key in frameKeys" :key="key"><span class="plabel">{{schema[key].label}}<span class="pval">{{fmt(state[key])}}</span></span><input v-model.number="state[key]" type="range" :min="schema[key].min" :max="schema[key].max" :step="schema[key].step" /></div><button class="btn" @click="resetFrame">Сбросить кадр EQ</button></div></details><div class="px-4 py-4"><template v-for="key in paramKeys" :key="key"><label v-if="current.params[key].type==='toggle'">{{current.params[key].label}}<input type="checkbox" v-model="params[key]" /></label><div v-else><span class="plabel">{{current.params[key].label}}<span class="pval">{{fmt(params[key])}}</span></span><input v-model.number="params[key]" type="range" :min="current.params[key].min" :max="current.params[key].max" :step="current.params[key].step" /></div></template></div></aside></template>
